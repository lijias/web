<?php
header('Content-Type:text/html;charset=utf-8');
/* 1. 封装将数组转为字符串(join)  
 * 2.封装 $arr1=array(3,4,8,9,2);
 *       $arr2=array(12,7,8,9,3);
 *       将两个数组中相同的值组成一个新数组
 * 3.封装 
 *      $str ='b';
 *      $arr=array('one'=>'a','two'=>'b')
 *      查找 $str在数组中的值并且返回 该值的键和值的新数组
 * 4. 递归实现    
 *    求 1-n的平方和
 */